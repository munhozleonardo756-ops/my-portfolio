const theme = {
  colors: {
    // Fundo geral da página
    background: "#0B1220", // azul bem escuro

    // Áreas de conteúdo (cards / seções claras)
    surface: "#FFFFFF",
    surfaceMuted: "#F3F6FD",

    // Primárias em tons de azul
    primary: "#1D70E5", // azul principal
    primaryDark: "#124B9E", // hover / estados ativos
    primarySoft: "#E3F0FF", // fundos suaves, badges, etc.

    // Cor de destaque (pra detalhes, hover secundário etc.)
    accent: "#FFB156", // laranja suave que combina com azul

    // Textos
    text: "#0F172A", // texto em superfícies claras
    textOnPrimary: "#FFFFFF",
    subtleText: "#6B7280", // descrições, textos fracos
    muted: "#9CA3AF", // labels, legendas

    // Bordas / linhas
    border: "rgba(15, 23, 42, 0.12)",
  },

  fonts: {
    body: "'Inter', sans-serif",
  },
};

export default theme;
